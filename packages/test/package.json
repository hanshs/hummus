{
  "name": "@hummus/test",
  "author": "Hans Starkopf",
  "version": "1.0.0",
  "scripts": {
    "test": "yarn gen && playwright test hummus.spec.ts",
    "gen": "yarn with-env tsx ./src/generate.ts",
    "with-env": "dotenv -e ../../.env --"
  },
  "dependencies": {
    "@hummus/api": "*"
  },
  "devDependencies": {
    "@playwright/test": "^1.22.2",
    "@trpc/client": "^10.8.1",
    "@trpc/server": "^10.8.1",
    "abort-controller": "^3.0.0",
    "node-fetch": "^3.3.0",
    "tsx": "^3.12.2"
  }
}
