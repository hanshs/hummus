{
  "name": "@hummus/db",
  "version": "0.1.0",
  "main": "./index.ts",
  "types": "./index.ts",
  "license": "MIT",
  "scripts": {
    "clean": "rm -rf .turbo node_modules",
    "db:generate": "pnpm with-env prisma generate ",
    "db:push": "pnpm with-env prisma db push --skip-generate --accept-data-loss --force-reset",
    "dev": "pnpm with-env prisma studio --port 5556",
    "with-env": "dotenv -e ../../.env --"
  },
  "dependencies": {
    "@prisma/client": "^4.8.0",
    "@types/bcrypt": "^5.0.0"
  },
  "devDependencies": {
    "@types/node": "^18.0.0",
    "bcrypt": "^5.1.0",
    "dotenv-cli": "^6.0.0",
    "prisma": "^4.8.0",
    "ts-node": "^10.9.1",
    "typescript": "^4.9.4"
  },
  "prisma": {
    "seed": "ts-node --compiler-options {\"module\":\"commonjs\"} ./seed.ts"
  }
}
